# Office Hours — Space Dandy Game

**Autonomous worker:** every 2h weekdays 10:00–18:00 (see `agent-corps/fleet/repo-registry.json`).

**Pickup:** read `WAVES.md` — first pending wave, one PR per wave. Branch: `automation/wave-*`.

**Verify:** `npm run verify` (tsc + vitest + vite build + HTML check).

**Aesthetics:** neon pink/cyan, space grid, CRT scanline toggle, responsive Phaser scale FIT.

**Never merge your own PR.** Owner merges when green.
